# Frontend (React + Vite)

Основной запуск проекта описан в backend README:

- [backend/README.md](../backend/README.md)

Коротко:

1. Перейти в папку `backend`.
2. Выполнить `docker compose up --build -d`.
3. Открыть frontend: `http://127.0.0.1:5173`.

