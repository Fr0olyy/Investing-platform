# Документация разработки

## Цели backend-а

Backend должен поддерживать 3 основных домена:

1. Пользователь и виртуальный брокерский счет.
2. Рыночные данные и торговые операции.
3. ML-подсистема для прогнозов и сценарного анализа.

## Принципы проекта

- Frontend и backend разделены.
- Бизнес-логика не должна жить в роутерах.
- Все DTO и контракты должны описываться через Pydantic.
- Все изменения в поведении API должны быть отражены в Swagger.
- ML-часть должна быть заменяемой без слома REST-контрактов.

## Слои приложения

### `api`

Задачи:

- принять HTTP-запрос;
- провалидировать вход;
- вызвать сервис;
- вернуть ответ.

### `services`

Задачи:

- реализовать сценарии системы;
- работать с ORM-моделями;
- считать PnL, обновлять портфель, формировать прогнозы.

### `db`

Задачи:

- подключение к БД;
- ORM-модели;
- начальный seed данных.

### `schemas`

