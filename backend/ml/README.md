# ML Workspace

This folder is prepared for your own ML implementation.

- `artifacts/`: trained model files, explainers, serialized pipelines.
- `datasets/`: prepared datasets and feature tables.
- `experiments/`: notebooks, drafts and research outputs.

The backend already stores ML-related metadata in the database and exposes scenario/prediction endpoints through FastAPI.
